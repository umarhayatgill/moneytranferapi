import io.cucumber.junit.Cucumber;
import org.junit.runner.RunWith;

@RunWith(Cucumber.class)
public class MoneyTransferTest
{
    //this runner is just used to ignite acceptance tests, it is a convention of cucumber
}
